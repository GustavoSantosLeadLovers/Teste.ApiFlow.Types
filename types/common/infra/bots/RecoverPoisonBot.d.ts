import { RecoverPoisonData } from '../../services/RecoverPoisonService';
export default class RecoverPoisonBot {
    private readonly data;
    constructor(data: RecoverPoisonData);
    execute(): void;
}
